
<%= link_to "New post", new_post_path %>

<% @posts.each do |post| %>
<%= link_to post.title, post_path(post.created_at.strftime("%Y"),
  post.created_at.strftime("%m"), post.slug) %>
<%= post.text %>
<% end %>
